// show users an alert message about the combination
//declare first variable using values that equals 10
//declare second variable using values that equals 40
//declare third variable using values that equals to 39
//console.log() or alert() the entire combo


alert("You have received this message because you have been chosen to open an important vault. Here is the secret combination:");

const firstCombo = 5 + 5;
const secondCombo = 8 * 5;
const thirdCombo = 40 - 1;
const comboFinal = `${firstCombo} ${secondCombo} ${thirdCombo}`;
// another way to to do this instead of creating another variable is to console.log (firstCombo, secondCombo, thirdCombo);

alert(comboFinal);
